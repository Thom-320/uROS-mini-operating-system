#pragma once

// uROS configuration defaults
// Set to 1 for preemptive scheduling (timer-driven context switches)
// Set to 0 for cooperative scheduling (manual yield points)
#define CONFIG_PREEMPT_DEFAULT 0

